language: julia
os:
  - linux
  - osx
julia:
  - 0.4
  # - nightly   # temporarily disable this, until the tuple stuff is sorted out
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("EmpiricalRisks"); Pkg.test("EmpiricalRisks"; coverage=true)'
